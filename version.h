// version.h 🏷️ Версия прошивки — единая точка правды.
// ⚠️ Обновлять при КАЖДОМ релизе (git tag vX.Y.Z должен совпадать с FW_VERSION).
// Значение уходит в чат при старте системы — так видно, применилась ли OTA.
#pragma once

#define FW_VERSION "1.9.1"
